# Najotkutubxona — Online Library Backend

## Project description
This project is a backend database design for an online library system.
PostgreSQL is used as the main database.

## Database structure
- users
- authors
- genres
- books
- comments

## How to run
1. Create a PostgreSQL database
2. Run `database.sql` file:
   psql -d database_name -f database.sql
